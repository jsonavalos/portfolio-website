<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= name %></title>
    <link rel="icon" href="<%= icon_image %>" type="image/x-icon">
    <!-- Tailwind CSS (via CDN for simplicity) -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet" />
    <!-- Font Awesome for Icons -->
    <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
    <!-- AOS for scroll animations -->
    <link href="https://cdn.jsdelivr.net/npm/aos@2.3.4/dist/aos.css" rel="stylesheet" />
    <style>
      body { font-family: 'Inter', sans-serif; }
      .gradient-bg { background: linear-gradient(to bottom right, #1e3a8a, #6366f1); }
    </style>
  </head>
  <body class="text-gray-100 bg-gray-950">
    <!-- Hero Section -->
    <header class="gradient-bg min-h-screen flex flex-col justify-center items-center text-center p-8" data-aos="fade-down">
      <div class="w-48 h-48 mb-6 rounded-full overflow-hidden border-4 border-white shadow-lg">
        <img src="/static/images/javalos.jpeg" alt="Profile picture" class="w-full h-full object-cover" />
      </div>
      <h1 class="text-5xl md:text-7xl font-extrabold tracking-tight mb-4 drop-shadow-lg"><%= name %></h1>
      <p class="text-xl md:text-3xl mb-8 font-light text-gray-200"><%= title %></p>
      <a href="https://github.com/jsonavalos?tab=repositories" target="_blank" style="visibility: hidden;" class="inline-flex items-center gap-2 bg-white text-gray-900 px-6 py-3 rounded-full font-semibold shadow-xl transition hover:scale-105">
        <i class="fas fa-arrow-down" ></i> See My Work
      </a>
    </header>

    <!-- About Section -->
    </section>
    <br>
      <h2 class="text-4xl font-bold mb-6 text-center">About Me</h2>
      <p class="leading-relaxed text-lg text-center max-w-3xl mx-auto"><%= about %></p>
    </section>

    <!-- Education Section -->
    <section id="education" class="max-w-4xl mx-auto px-6 py-20" data-aos="fade-up">
      <h2 class="text-4xl font-bold mb-6 text-center">Education</h2>
      <div class="space-y-4 text-lg text-center">
        <p>
          <strong>Master of Science</strong> in Applied Data Science - University of San Diego – Aug 2026<br/>
          <span class="text-sm text-gray-400">Focus: Machine Learning,Data Analysis, Time Series, Big Data, Cloud Computing</span>
        </p>
        <p>
          <strong>Bachelor of Science</strong> in Mathematics & Computer Science - UC San Diego<br/>
          <span class="text-sm text-gray-400">Graduated with a strong foundation in algorithms, probability, and software development</span>
        </p>
      </div>
    </section>


    <!-- Projects Section -->
    <section id="projects" class="bg-gray-900 py-20" data-aos="fade-up">
      <div class="max-w-6xl mx-auto px-6">
        <h2 class="text-4xl font-bold mb-12 text-center">Featured Projects</h2>
        <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-10">
          <% projects.forEach(project => { %>
          <article class="bg-gray-800 rounded-2xl overflow-hidden shadow-lg hover:shadow-2xl transition transform hover:-translate-y-1" data-aos="zoom-in">
            <img src="<%= project.image %>" alt="<%= project.title %> screenshot" class="w-full h-48 object-cover" />
            <div class="p-6 flex flex-col h-full">
              <h3 class="text-2xl font-semibold mb-2 text-indigo-300"><%= project.title %></h3>
              <p class="text-gray-300 mb-2"><%= project.description %></p>

<div class="flex flex-wrap gap-2 mb-3">
  <% project.tags.forEach(tag => { %>
    <span class="bg-indigo-600 text-xs px-3 py-1 rounded-full font-medium"><%= tag %></span>
  <% }) %>
</div>

<div class="text-center">
  <a href="<%= project.link %>" target="_blank" 
     class="text-indigo-400 font-semibold hover:text-indigo-200">
     🔗 View Project →
  </a>
</div>

            </div>
          </article>
          <% }) %>
        </div>
      </div>
    </section>

    <!-- Skills Section -->
    <section id="skills" class="max-w-5xl mx-auto px-6 py-20" data-aos="fade-up">
      <h2 class="text-4xl font-bold mb-8 text-center">Technical Skills</h2>
      <div class="flex flex-wrap justify-center gap-4">
        <% skills.forEach(skill => { %>
        <span class="bg-gray-800 py-2 px-4 rounded-full text-sm font-semibold border border-gray-700"><%= skill %></span>
        <% }) %>
      </div>
    </section>

    <!-- Contact Section -->
    <section id="contact" class="bg-gray-900 py-20" data-aos="fade-up">
      <div class="max-w-3xl mx-auto px-6 text-center">
        <h2 class="text-4xl font-bold mb-6">Get in Touch</h2>
        <p class="mb-8 text-lg text-gray-300">I'm open to new collaborations, freelance gigs, or full-time roles. Let's connect and build something amazing!</p>
        <a href="mailto:<%= email %>" class="inline-flex items-center gap-2 bg-indigo-600 px-8 py-3 rounded-full font-semibold hover:bg-indigo-700 transition">
          <i class="fas fa-envelope"></i> Email Me
        </a>
      </div>
    </section>

  

    <!-- AOS init -->
    <script src="https://cdn.jsdelivr.net/npm/aos@2.3.4/dist/aos.js"></script>
    <script>AOS.init({ once: true, duration: 800 });</script>
  </body>
</html>
